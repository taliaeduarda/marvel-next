import { container } from "../../utils/global-definitions";

const styles = {
  container: {
    zIndex: "12",
    color: "#FFFFFF",
    ...container,
  },
    gridItem: {
    position: "relative",
    marginLeft: '20px',
    marginRight: '20px',
    minHeight: "1px",
    flexBasis: "auto",
  },
  };

  export default styles;